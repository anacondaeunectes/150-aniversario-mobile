import { FormateoCuentaAtrasPipe } from './formateo-cuenta-atras.pipe';

describe('FormateoCuentaAtrasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoCuentaAtrasPipe();
    expect(pipe).toBeTruthy();
  });
});
