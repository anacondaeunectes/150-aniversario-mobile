import { FormateoCDPipe } from './formateo-cd.pipe';

describe('FormateoCDPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoCDPipe();
    expect(pipe).toBeTruthy();
  });
});
